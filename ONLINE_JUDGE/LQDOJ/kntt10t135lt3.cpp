#include <bits/stdc++.h>
using namespace std;

#define NAME "kntt10t135lt3"
#define ln '\n'

typedef long long ll;
typedef long double ld;

void docfile() {
	if (ifstream(NAME".inp")) {
		freopen(NAME".inp", "r", stdin);
		freopen(NAME".out", "w", stdout);
	}
}

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
	docfile();

	string ho_ten; getline(cin, ho_ten);
	int c; cin >> c;

	for (char c : ho_ten) {
		if (c == 0)
		}

	return 0;
}