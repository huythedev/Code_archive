#include <iostream>
#include <vector>
#include <numeric>

// Mảng parent để lưu cha của mỗi phần tử


int main() {
    // Tăng tốc độ nhập xuất
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);

    
    }

    return 0;
}
