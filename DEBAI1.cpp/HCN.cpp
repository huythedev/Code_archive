#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
	
	ll C;
	cin >> C;
	
	cout << fixed << setprecision(2) << pow((C / 4.0), 2);

    return 0;
}