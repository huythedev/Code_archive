#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
	
	vector<ll> a, b;
	ll p, n, m;

	cin >> n;
	while(n--) {
		ll x;
		cin >> x;
		a.push_back(x);
	}
	cin >> m;
	while(m--) {
		ll x;
		cin >> x;
		b.push_back(x);
	}
	cin >> p;
	
    return 0;
}