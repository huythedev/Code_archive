#include <bits/stdc++.h>
using namespace std;

int main() {
    long long T;
    cin >> T;

    vector<string> vec;
    while(T--) {
        string s;
        cin >> s;
        vec.push_back(s);
    }

    for (string st : vec) {
        if (st.length() == 1) {
            cout << "1";
        }
        else {
            long long pos = -1;
            
        }
    }
    return 0;
}